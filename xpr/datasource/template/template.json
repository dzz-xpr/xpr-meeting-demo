{
    "adapter" : "XprApiAdapter",
    "config" : {
        "uri" : "/articles/",
        "params" : {
            "related_Section_Id__eq" : "{{options.ListingSectionId}}"
         }
    },
    "scripts" : [
        {
            "path" : "/utils/to_uppercase",
            "options" : { "field", "Title" }
        }
    ]
}
